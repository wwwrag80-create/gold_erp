# إلزامي لتشغيل الواجهات
PyQt5>=5.15

# اختياري: صور الباركود وQR (النظام يعمل بدونها ويعرض الأرقام/النص بدلاً من الصور)
python-barcode>=0.15
qrcode>=7.4
Pillow>=10.0

# للتحزيم كملف exe فقط
# pyinstaller>=6.0
