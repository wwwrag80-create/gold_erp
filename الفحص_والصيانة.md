# الفحص والصيانة

أدوات التحقق من سلامة النظام وصيانته. كلها تُشغَّل من مجلد المشروع
ولا تمسّ بيانات المستخدم إطلاقاً (الفحوص تبني قواعد مؤقتة وتحذفها).

---

## قبل أي تسليم أو تحديث

شغّل الثلاثة بالترتيب. كلها يجب أن تنتهي بـ «سليم».

```
python tools/selfcheck.py     # فحص وقائي بنيوي (أسرار، عقود، تغطية)
python tools/audit.py         # صائد الأخطاء الصامتة
python tools/verify_all.py    # ترجمة + بناء كل شاشة + استدعاء كل معالج
python tools/smoke_flow.py    # دورة مصنع كاملة والتحقق من الأرقام
```

### ما يفحصه كلٌّ منها

| الأداة | ما تُثبته |
|---|---|
| `selfcheck.py` | لا أسرار في المستودع · عقود الخادم متطابقة · مسارات الأخطاء مغطّاة |
| `audit.py` | لا استثناءات مبتلعة في الحفظ · لا نداءات محجوبة على الواجهة |
| `verify_all.py` | كل ملف يُترجم · كل شاشة تُبنى وتُفتح · كل زر يُستدعى بلا خطأ |
| `smoke_flow.py` | **الأرقام صحيحة**: توريد ← بيع ← سند ← ميزان ← قفل ← حذف |

الفرق بين الأخيرتين مهم: `verify_all` يُثبت أن النظام **يعمل**،
و`smoke_flow` يُثبت أن ما يخرج منه **صحيح**. الأول لا يغني عن الثاني.

---

## قياس الأداء

```
python tools/bench.py 20000       # يبني دفتراً بـ 20 ألف قيد ويقيس
```

يقيس زمن المسارات التي يلمسها المستخدم يومياً: فتح معاملة، رصيد
حساب، لوحة التحكم، كشف حساب، فحص السلامة، طابور المزامنة.

شغّله **قبل** أي تعديل على طبقة قاعدة البيانات وبعده وقارن — فلا
يُقال «صار أسرع» بلا رقم.

---

## صيانة قاعدة البيانات

قاعدة SQLite لا تبطؤ فجأة بل ببطء، لثلاثة أسباب: تقادم إحصاءات
مخطِّط الاستعلام، تضخّم ملف WAL، وبقاء الصفحات المحرَّرة في الملف.

**تلقائياً**: دورة يومية خفيفة تعالج الثلاثة (تبدأ بعد خمس دقائق من
الإقلاع). آمنة أثناء العمل ولا يشعر بها المستخدم.

**يدوياً** من شاشة «النظام السحابي وإدارة المصانع» ← تبويب الصيانة:

| الزر | متى |
|---|---|
| ⚡ صيانة سريعة | عند الشعور ببطء — ثوانٍ، آمن أثناء العمل |
| 🗜 ضغط قاعدة البيانات | إن كبر حجم الملف — **يقفل النظام**، خارج وقت العمل |
| 📊 حجم الجداول | لمعرفة أي جدول كبر ولماذا |

---

## قفل الفترات المالية

من شاشة «الإقفال السنوي» ← «قفل الفترات المحاسبية».

بعد تسليم ميزانية فترة أو إقرارها الضريبي، اقفلها. عندئذٍ يُرفض أي
قيد أو حذف بتاريخ داخلها، فلا تتغيّر أرقام سبق اعتمادها بسبب خطأ في
حقل التاريخ. رفع القفل متاح وقت الحاجة، وكل رفع أو إنزال يُسجَّل في
سجل التدقيق باسم من نفّذه.

---

## الأسرار

ملف `.env` يحمل مفاتيح Supabase، ومنها **مفتاح الخدمة**
(`SUPABASE_SERVICE_KEY`) الذي يتجاوز كل صلاحيات RLS: من يملكه يقرأ
ويكتب بيانات كل المصانع.

- لا يدخل المستودع (`.gitignore` يستثنيه، و`selfcheck.py` يتحقق).
- **لا يُوزَّع مع نسخة المصنع** — نسخة المصنع تحتاج
  `SUPABASE_PUBLISHABLE_KEY` وحده.
- `.env.example` هو القالب المُشارك، بقيم وهمية فقط.

إن تسرّب المفتاح يوماً: أبطِله من لوحة Supabase وأصدر بديلاً — تغيير
الملف وحده لا يكفي، لأن المفتاح القديم يبقى صالحاً حتى يُبطَل.
